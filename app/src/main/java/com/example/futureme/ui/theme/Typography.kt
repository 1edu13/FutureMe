package com.example.futureme.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.example.futureme.R

/* 1. Definimos la familia de fuentes (Asegúrate de bajar los .ttf de Montserrat)
val Montserrat = FontFamily(
    Font(R.font.montserrat_light, FontWeight.Light),
    Font(R.font.montserrat_regular, FontWeight.Normal),
    Font(R.font.montserrat_medium, FontWeight.Medium),
    Font(R.font.montserrat_semibold, FontWeight.SemiBold),
    Font(R.font.montserrat_bold, FontWeight.Bold)
)

// 2. Configuramos el sistema de tipografía
val AppTypography = Typography(

    // Títulos grandes (Ej: "Bienvenido de Nuevo")
    displayLarge = TextStyle(
        fontFamily = Montserrat,
        fontWeight = FontWeight.Light,
        fontSize = 32.sp,
        letterSpacing = 1.5.sp, // Esto le da el toque premium
        lineHeight = 40.sp
    ),

    // Títulos de tarjetas (Ej: "Mis Cápsulas")
    titleLarge = TextStyle(
        fontFamily = Montserrat,
        fontWeight = FontWeight.SemiBold,
        fontSize = 20.sp,
        letterSpacing = 0.5.sp
    ),

    // Texto de botones (Ej: "INICIAR SESIÓN")
    labelLarge = TextStyle(
        fontFamily = Montserrat,
        fontWeight = FontWeight.Medium,
        fontSize = 14.sp,
        letterSpacing = 1.25.sp // Espaciado para que se vea más moderno
    ),

    // Texto de cuerpo (Descripciones)
    bodyLarge = TextStyle(
        fontFamily = Montserrat,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        letterSpacing = 0.25.sp,
        lineHeight = 24.sp
    ),

    // Texto pequeño (Ej: "Se abre el: Dec 29...")
    bodySmall = TextStyle(
        fontFamily = Montserrat,
        fontWeight = FontWeight.Normal,
        fontSize = 12.sp,
        letterSpacing = 0.4.sp,
        color = GrisAzulado // Usando el color de tu paleta
    )
)*/